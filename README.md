Steps for compiling
-------------------

Fetch imgui-sfml (added as a submodule to this project):

1. `git submodule init`
2. `git submodule update`

Then build by

4. `mkdir build`
5. `cd build`
6. `cmake ..`
7. `make`
